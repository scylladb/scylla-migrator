source:
  type: dynamodb
  table: RenamedItems
  region: dummy
  endpoint:
    host: dynamodb
    port: 8000
  credentials:
    accessKey: dummy
    secretKey: dummy
  maxMapTasks: 1

target:
  type: dynamodb
  table: RenamedItems
  region: dummy
  endpoint:
    host: scylla
    port: 8000
  credentials:
    accessKey: dummy
    secretKey: dummy
  maxMapTasks: 1
  streamChanges: false
  removeConsumedCapacity: true
  billingMode: PAY_PER_REQUEST

renames:
  - from: foo
    to: quux

# Below are unused but mandatory settings
savepoints:
  path: /app/savepoints
  intervalSeconds: 300
